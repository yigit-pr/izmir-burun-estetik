---

---

<section
  id="hero"
  class="relative min-h-screen flex items-center bg-primary overflow-hidden"
>
  <!-- Background Elements -->
  <div class="absolute inset-0 z-0">
    <div
      class="absolute inset-0 bg-gradient-to-br from-primary via-slate-900 to-primary/95"
    >
    </div>
    <!-- Grain/Noise Texture Placeholder -->
    <div
      class="absolute inset-0 opacity-[0.03] pointer-events-none mix-blend-overlay bg-[url('https://www.transparenttextures.com/patterns/asfalt-light.png')]"
    >
    </div>
  </div>

  <div
    class="container mx-auto px-4 relative z-10 grid md:grid-cols-2 gap-16 items-center pt-20"
  >
    <!-- Text Content -->
    <div class="text-white space-y-8 animate-fade-in">
      <div class="space-y-4">
        <span
          class="inline-block text-accent tracking-[0.3em] uppercase text-xs font-bold mb-2 opacity-0 animate-slide-right"
          style="animation-delay: 0.2s; animation-fill-mode: forwards;"
        >
          İzmir'in En Seçkin Estetik Kliniği
        </span>
        <h1
          class="text-6xl md:text-8xl font-serif font-light leading-[1.1] opacity-0 animate-slide-up"
          style="animation-delay: 0.4s; animation-fill-mode: forwards;"
        >
          Doğal Güzellik, <br />
          <span class="italic text-accent">Karakterli</span> Bakış.
        </h1>
      </div>

      <p
        class="text-lg md:text-xl text-gray-300/90 font-light max-w-lg leading-relaxed opacity-0 animate-slide-up"
        style="animation-delay: 0.6s; animation-fill-mode: forwards;"
      >
        Yüzünüzün özgünlüğünü koruyan, fonksiyonel ve estetik açıdan kusursuz
        burun tasarımları ile yeni bir başlangıca adım atın.
      </p>

      <div
        class="flex flex-col sm:flex-row gap-5 pt-4 opacity-0 animate-slide-up"
        style="animation-delay: 0.8s; animation-fill-mode: forwards;"
      >
        <a
          href="#contact"
          class="group relative inline-flex justify-center items-center px-10 py-5 bg-accent text-primary font-bold rounded-full overflow-hidden transition-all duration-500 hover:shadow-[0_0_30px_rgba(255,215,0,0.3)]"
        >
          <span class="relative z-10">Ücretsiz Analiz Alın</span>
          <div
            class="absolute inset-0 bg-white translate-y-full group-hover:translate-y-0 transition-transform duration-500"
          >
          </div>
        </a>
        <a
          href="#before-after"
          class="inline-flex justify-center items-center px-10 py-5 border border-white/20 text-white font-medium rounded-full hover:bg-white/5 transition-all duration-300 backdrop-blur-sm"
        >
          Sonuçları Görün
        </a>
      </div>

      <div
        class="pt-12 flex items-center gap-10 text-[10px] tracking-[0.2em] uppercase text-gray-400 opacity-0 animate-fade-in"
        style="animation-delay: 1s; animation-fill-mode: forwards;"
      >
        <div class="flex items-center gap-3">
          <span class="w-1.5 h-1.5 rounded-full bg-accent/60"></span>
          <span>Op. Dr. Onaylı</span>
        </div>
        <div class="flex items-center gap-3">
          <span class="w-1.5 h-1.5 rounded-full bg-accent/60"></span>
          <span>Kişiye Özel Tasarım</span>
        </div>
      </div>
    </div>

    <!-- Image Content -->
    <div class="relative hidden md:flex justify-center items-center h-full">
      <div
        class="relative w-[500px] h-[650px] rounded-[100px] overflow-hidden border border-white/10 shadow-2xl opacity-0 animate-scale-in"
        style="animation-delay: 0.5s; animation-fill-mode: forwards;"
      >
        <img
          src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?q=80&w=1000&auto=format&fit=crop"
          alt="İzmir Burun Estetiği - Doğal Profil"
          class="w-full h-full object-cover grayscale-[10%] hover:scale-110 transition-transform duration-[2000ms]"
        />
        <div
          class="absolute inset-0 bg-gradient-to-t from-primary/60 via-transparent to-transparent"
        >
        </div>
      </div>
      <!-- Decorative background shapes -->
      <div
        class="absolute -z-10 top-0 -right-20 w-80 h-80 bg-accent/10 rounded-full blur-[120px] animate-pulse-slow"
      >
      </div>
      <div
        class="absolute -z-10 bottom-0 -left-20 w-64 h-64 bg-slate-400/10 rounded-full blur-[100px]"
      >
      </div>
    </div>
  </div>

  <!-- Scroll Down Indicator -->
  <div
    class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce hidden md:block"
  >
    <svg
      class="w-6 h-6 text-white/50"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>
</section>

<style>
  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes slide-right {
    from {
      opacity: 0;
      transform: translateX(-20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.95);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
  @keyframes pulse-slow {
    0%,
    100% {
      opacity: 0.1;
      transform: scale(1);
    }
    50% {
      opacity: 0.15;
      transform: scale(1.1);
    }
  }
  .animate-slide-up {
    animation: slide-up 1.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }
  .animate-slide-right {
    animation: slide-right 1.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }
  .animate-fade-in {
    animation: fade-in 2s ease-out forwards;
  }
  .animate-scale-in {
    animation: scale-in 1.5s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }
  .animate-pulse-slow {
    animation: pulse-slow 8s infinite ease-in-out;
  }
</style>
